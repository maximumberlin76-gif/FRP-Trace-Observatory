"""Read-only parsers for published Fractal Resonance Processor artifacts.

This package preserves original source bytes, validates artifacts against the
Observatory compatibility registry, and produces normalized read-only records.

It does not execute upstream producers, modify source artifacts, or define
processor semantics.
"""
